import React from 'react';
import { View, Image, Text, TouchableOpacity } from 'react-native';

const Notification = ({ navigation }) => {
    return (
        <>
            <View>
                <Text style={{ fontSize: 30 }}>Welcome Notification</Text>
            </View>
        </>
    );
};

export default Notification;

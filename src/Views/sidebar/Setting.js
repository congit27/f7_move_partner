import React from 'react';
import { View, Image, Text, TouchableOpacity } from 'react-native';

const Help = ({ navigation }) => {
    return (
        <>
            <View>
                <Text style={{ fontSize: 30 }}>Welcome Setting</Text>
            </View>
        </>
    );
};

export default Help;
